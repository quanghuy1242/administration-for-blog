import { HeighlightCodeDirective } from './heighlight-code.directive';

describe('HeighlightCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeighlightCodeDirective();
    expect(directive).toBeTruthy();
  });
});
